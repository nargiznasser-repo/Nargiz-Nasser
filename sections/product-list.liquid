{% liquid
  assign max_items = section.settings.max_products
  assign section_products = section.settings.collection.products
  assign max_items = section_products.size | at_most: section.settings.max_products
%}
<link rel="stylesheet" href="{{ 'styles.css' | asset_url }}">
<script src="{{ 'product-card.js' | asset_url }}" defer></script>

<div
  id="product-card-section"
  class="section section--{{ section.settings.section_width }} color-{{ section.settings.color_scheme }}"
>
  <div
    class="product-list-grid"
    style="display: grid; grid-template-columns: repeat({{ section.settings.columns }}, 1fr); gap: {{ section.settings.columns_gap }}px;"
  >
    {% for i in (1..max_items) %}
      {% assign index = forloop.index0 %}
      {% assign product = section_products[index] %}
      {% if product %}
        <div class="product-card">
          {% content_for 'block', type: '_product-card', id: 'static-product-card', closest.product: product %}
        </div>
      {% endif %}
    {% endfor %}
  </div>
</div>

<!-- Product Details Modal -->
<div
  id="popupModal"
  class="fixed inset-0 bg-black/30 backdrop-blur-md hidden items-center justify-center z-50 transition-all w-200 h-447">
  <div class="bg-white shadow-xl p-6 relative">
    <button
      id="closeModalBtn"
      class="absolute top-3 right-3 text-gray-500 hover:text-gray-800 text-xl"
      onclick="closeModal()"
    >
      ✕
    </button>
    <div class="flex gap-4">
      <div class="flex-shrink-0 w-32 h-32">
        <img id="modalImage" src="" alt="" class="w-full h-full object-cover" width="128" height="128">
      </div>
      <div class="flex-1">
        <h2 id="modalTitle" class="text-lg font-semibold text-gray-800"></h2>
        <p id="modalPrice" class="text-gray-700 text-base mt-1"></p>
        <div id="modalDesc" class="text-gray-500 text-sm mt-2 leading-5"></div>
      </div>
    </div>
    <div class="mt-5">
      <h3 class="text-sm font-semibold text-gray-700 mb-2">Color</h3>
      <div id="modalColors" class="flex gap-0"></div>
    </div>
    <div class="mt-5">
      <h3 class="text-sm font-semibold text-gray-700 mb-2">Size</h3>
      <div class="relative w-full">
        <select
          id="modalSizes"
          class="appearance-none border border-black px-3 py-3 w-full text-base font-medium text-gray-900 bg-white cursor-pointer focus:outline-none focus:ring-2 focus:ring-black focus:border-black pr-12"
        >
          <option value="" disabled selected>Choose your size</option>
        </select>

        <!-- Vertical Divider -->
        <span class="absolute top-1/2 right-10 transform -translate-y-1/2 h-full w-px bg-black pointer-events-none"></span>

        <!-- Custom Arrow -->
        <svg
          class="pointer-events-none absolute top-1/2 right-3 w-4 h-4 text-black transform -translate-y-1/2"
          fill="none"
          stroke="currentColor"
          stroke-width="2"
          viewBox="0 0 24 24"
        >
          <path stroke-linecap="round" stroke-linejoin="round" d="M19 9l-7 7-7-7" />
        </svg>
      </div>
    </div>
    <button
      id="addToCartBtn"
      onclick="addToCart()"
      class="shop-now-btn mt-6 w-full py-3 flex justify-center items-center gap-2"
    >
      ADD TO CART
      <span class="arrow-icon">
        <svg width="24" height="8" viewBox="0 0 24 8" fill="none" xmlns="http://www.w3.org/2000/svg">
          <line x1="0" y1="4" x2="22" y2="4" stroke="#fff" stroke-width="1.5"/>
          <path d="M18 0 L24 4 L18 8" stroke="#fff" stroke-width="1.5" fill="none"/>
        </svg>
      </span>
    </button>
  </div>
</div>

<!-- Cart Drawer -->
<div
  id="drawer"
  class="fixed top-0 right-0 w-96 h-full bg-white shadow-2xl z-50 hidden flex flex-col"
>
  <div class="flex items-center justify-between px-6 py-4 border-b">
    <h2 class="text-lg font-bold text-gray-900">Shopping Cart</h2>
    <button id="closeDrawerBtn" onclick="closeCart()" class="p-2 rounded-full hover:bg-gray-100 transition">✕</button>
  </div>
  <div class="flex-1 overflow-y-auto px-6 py-4">
    <div id="cartGrid" class="space-y-4"></div>
  </div>
  <div class="border-t border-gray-200 px-6 py-5 bg-gray-50">
    <div class="flex justify-between items-center text-base font-semibold text-gray-900">
      <p>Subtotal</p>
      <p id="subtotalPrice">€0.00</p>
    </div>
    <p class="mt-1 text-sm text-gray-500">Shipping and taxes calculated at checkout.</p>
    <button
      id="checkoutBtn"
      class="mt-6 w-full bg-black text-white py-3 text-sm tracking-wide hover:bg-gray-900 transition flex justify-center items-center gap-2"
    >
      Proceed to Checkout
    </button>
  </div>
</div>

{% schema %}
{
  "name": "Product List Grid",
  "class": "ui-test-product-list",
  "settings": [
    {
      "type": "collection",
      "id": "collection",
      "label": "Collection"
    },
    {
      "type": "range",
      "id": "max_products",
      "label": "Max products",
      "min": 1,
      "max": 16,
      "step": 1,
      "default": 4
    },
    {
      "type": "range",
      "id": "columns",
      "label": "Columns",
      "min": 1,
      "max": 8,
      "step": 1,
      "default": 4
    },
    {
      "type": "range",
      "id": "columns_gap",
      "label": "Column gap (px)",
      "min": 0,
      "max": 100,
      "step": 1,
      "default": 8
    },
    {
      "type": "select",
      "id": "section_width",
      "label": "Section width",
      "options": [
        { "value": "page-width", "label": "Page width" },
        { "value": "full-width", "label": "Full width" }
      ],
      "default": "page-width"
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "Color scheme",
      "default": "scheme-1"
    }
  ]
}
{% endschema %}
